# Insecure Virtual SEV SNP report and keys
This folder contains SEV SNP report and corresponding public/private key pairs extracted from a real 
deployment that are then used in insecure/virtual test environments which do not run on SEV SNP hardware.

To refresh the report/keys used for testing run:
```pwsh
./generate-all.ps1
```
