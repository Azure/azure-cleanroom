[project]
name = "analytics-contracts"
version = "0.1.0"
description = "Shared contracts for analytics workload and frontend"
requires-python = ">=3.10"
dependencies = [
    "pydantic==2.11.7",
]

[build-system]
requires = ["uv_build>=0.9.3,<0.10.0"]
build-backend = "uv_build"
